services:
  pathway:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      HOST_IP: ${HOST_IP}
    restart: unless-stopped
