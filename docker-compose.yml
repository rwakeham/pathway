services:
  pathway:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    # environment:
    #   HOST_IP: 192.168.1.100  # Set if auto-detected URLs use the wrong IP (e.g. 0.0.0.0)
    restart: unless-stopped
